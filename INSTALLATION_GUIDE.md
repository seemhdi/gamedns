# راهنمای نصب و استفاده از GameDNS

## 📋 فهرست مطالب
1. [پیش‌نیازها](#پیش-نیازها)
2. [نصب Android Studio](#نصب-android-studio)
3. [باز کردن پروژه](#باز-کردن-پروژه)
4. [ساخت برنامه](#ساخت-برنامه)
5. [اجرا روی دستگاه](#اجرا-روی-دستگاه)
6. [راهنمای استفاده](#راهنمای-استفاده)
7. [عیب‌یابی](#عیب-یابی)

## 🔧 پیش‌نیازها

### نرم‌افزارهای مورد نیاز
- **Android Studio**: نسخه Hedgehog (2023.1.1) یا جدیدتر
- **JDK**: نسخه 17 یا بالاتر
- **Android SDK**: API Level 34
- **دستگاه اندروید**: Android 7.0 (API 24) یا بالاتر

### بررسی نسخه JDK
```bash
java -version
```
باید نسخه 17 یا بالاتر را نشان دهد.

## 📥 نصب Android Studio

### Windows
1. از [سایت رسمی Android Studio](https://developer.android.com/studio) دانلود کنید
2. فایل `.exe` را اجرا کنید
3. مراحل نصب را دنبال کنید
4. Android SDK را نصب کنید

### macOS
1. فایل `.dmg` را دانلود کنید
2. Android Studio را به پوشه Applications بکشید
3. برنامه را باز کنید و SDK را نصب کنید

### Linux
```bash
sudo snap install android-studio --classic
```

## 📂 باز کردن پروژه

### روش 1: از طریق Android Studio
1. Android Studio را باز کنید
2. روی **"Open"** کلیک کنید
3. پوشه `GameDNS` را انتخاب کنید
4. روی **"OK"** کلیک کنید
5. منتظر بمانید تا Gradle Sync کامل شود

### روش 2: از طریق خط فرمان
```bash
cd /path/to/GameDNS
studio .
```

## 🔨 ساخت برنامه

### از طریق Android Studio
1. منوی **Build** → **Make Project** را انتخاب کنید
2. یا کلید میانبر: `Ctrl+F9` (Windows/Linux) یا `Cmd+F9` (Mac)

### از طریق خط فرمان
```bash
# Windows
gradlew.bat build

# macOS/Linux
./gradlew build
```

### ساخت نسخه Release
```bash
./gradlew assembleRelease
```

فایل APK در مسیر زیر قرار می‌گیرد:
```
app/build/outputs/apk/release/app-release.apk
```

## 📱 اجرا روی دستگاه

### فعال‌سازی Developer Options
1. **Settings** → **About Phone**
2. روی **Build Number** هفت بار ضربه بزنید
3. **Developer Options** فعال می‌شود

### فعال‌سازی USB Debugging
1. **Settings** → **Developer Options**
2. **USB Debugging** را فعال کنید

### اتصال دستگاه
1. دستگاه را با کابل USB به کامپیوتر وصل کنید
2. روی دستگاه، اجازه USB Debugging را بدهید
3. در Android Studio، دستگاه را از لیست انتخاب کنید
4. روی دکمه **Run** (▶️) کلیک کنید

### نصب APK به صورت دستی
```bash
adb install app/build/outputs/apk/debug/app-debug.apk
```

## 📖 راهنمای استفاده

### اولین اجرا

#### 1. اجازه VPN
- برنامه را باز کنید
- یک DNS انتخاب کنید
- روی **Connect** کلیک کنید
- اجازه VPN را بدهید (فقط یک بار)

#### 2. انتخاب DNS
- از لیست DNS سرورها یکی را انتخاب کنید
- یا روی **Find Best DNS** کلیک کنید تا بهترین DNS پیدا شود

#### 3. تست سرعت
- روی دکمه **Test** کنار هر DNS کلیک کنید
- نتایج شامل:
  - میانگین پینگ (Average Ping)
  - کمترین پینگ (Min Ping)
  - بیشترین پینگ (Max Ping)
  - کیفیت (Quality Rating)

### ویژگی‌های اصلی

#### اتصال به DNS
```
1. انتخاب DNS از لیست
2. کلیک روی Connect
3. تایید اجازه VPN
4. مشاهده وضعیت اتصال
```

#### یافتن بهترین DNS
```
1. کلیک روی "Find Best DNS"
2. منتظر تست همه DNSها بمانید
3. بهترین DNS به صورت خودکار انتخاب می‌شود
4. روی Connect کلیک کنید
```

#### مشاهده آمار
هنگام اتصال، آمار زیر نمایش داده می‌شود:
- **Ping**: پینگ فعلی
- **Duration**: مدت زمان اتصال
- **Data Used**: حجم داده مصرفی
- **Quality**: کیفیت اتصال

#### بازی‌های محبوب
```
1. روی آیکون Games کلیک کنید
2. بازی مورد نظر را انتخاب کنید
3. DNS پیشنهادی را ببینید
4. روی DNS مورد نظر کلیک کنید
```

### تنظیمات

#### تغییر تم
1. روی آیکون **Settings** کلیک کنید
2. **Theme** را انتخاب کنید:
   - Light (روشن)
   - Dark (تیره)
   - System (سیستم)

#### نوتیفیکیشن‌ها
- فعال/غیرفعال کردن نوتیفیکیشن‌ها
- نمایش وضعیت اتصال در نوار اعلان

## 🐛 عیب‌یابی

### مشکلات رایج

#### 1. برنامه Build نمی‌شود
**راه حل**:
```bash
# پاک کردن Build Cache
./gradlew clean

# Build مجدد
./gradlew build
```

#### 2. Gradle Sync Failed
**راه حل**:
- **File** → **Invalidate Caches / Restart**
- یا:
```bash
./gradlew --refresh-dependencies
```

#### 3. اجازه VPN داده نمی‌شود
**راه حل**:
- Settings → Apps → GameDNS → Permissions
- اجازه‌های لازم را بررسی کنید

#### 4. DNS متصل نمی‌شود
**راه حل**:
- اتصال اینترنت را بررسی کنید
- VPN دیگری فعال نباشد
- برنامه را مجدد راه‌اندازی کنید

#### 5. تست سرعت کار نمی‌کند
**راه حل**:
- اتصال اینترنت را بررسی کنید
- DNS سرور در دسترس باشد
- چند بار دوباره امتحان کنید

### لاگ‌های Debug

#### مشاهده لاگ‌ها در Android Studio
1. **View** → **Tool Windows** → **Logcat**
2. فیلتر: `GameDNS`

#### مشاهده لاگ‌ها از خط فرمان
```bash
adb logcat | grep GameDNS
```

### بررسی وضعیت VPN
```bash
adb shell dumpsys connectivity | grep VPN
```

## 🔄 به‌روزرسانی

### دریافت آخرین نسخه
```bash
git pull origin main
./gradlew clean build
```

### نصب نسخه جدید
- APK جدید را نصب کنید
- داده‌های قبلی حفظ می‌شود

## 📊 بهینه‌سازی

### کاهش حجم APK
```bash
./gradlew assembleRelease --scan
```

### بررسی Performance
1. **Run** → **Profile 'app'**
2. CPU, Memory, Network را بررسی کنید

## 🔐 امنیت

### تست امنیتی
```bash
./gradlew lint
```

### بررسی مجوزها
```bash
aapt dump permissions app/build/outputs/apk/debug/app-debug.apk
```

## 📞 پشتیبانی

### گزارش مشکل
1. لاگ‌ها را جمع‌آوری کنید
2. Issue در GitHub باز کنید
3. اطلاعات زیر را ارائه دهید:
   - نسخه اندروید
   - مدل دستگاه
   - نسخه برنامه
   - شرح مشکل
   - لاگ‌های مربوطه

### تماس با توسعه‌دهنده
- GitHub Issues
- Email: support@gamedns.com

## ✅ چک‌لیست نصب

- [ ] Android Studio نصب شده
- [ ] JDK 17+ نصب شده
- [ ] پروژه باز شده
- [ ] Gradle Sync موفق
- [ ] Build موفق
- [ ] برنامه روی دستگاه اجرا شد
- [ ] اجازه VPN داده شد
- [ ] DNS متصل شد
- [ ] تست سرعت کار می‌کند

## 🎓 منابع آموزشی

### مستندات
- [Android VPN Service](https://developer.android.com/develop/connectivity/vpn)
- [Jetpack Compose](https://developer.android.com/jetpack/compose)
- [Material Design 3](https://m3.material.io/)

### ویدیوهای آموزشی
- راهنمای استفاده از GameDNS
- نحوه یافتن بهترین DNS
- بهینه‌سازی برای بازی

---

## 🎉 موفق باشید!

اگر سوالی دارید، در بخش Issues پرسیده یا با ما تماس بگیرید.
